{
  "name": "image-background-remover",
  "version": "1.0.0",
  "description": "Remove background from images using Remove.bg API",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrancer preview"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}